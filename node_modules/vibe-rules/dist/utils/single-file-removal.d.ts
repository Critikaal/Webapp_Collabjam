/**
 * Removes a tagged rule block from a single file
 */
export declare function removeTaggedRuleFromFile(filePath: string, ruleName: string): Promise<boolean>;
/**
 * Removes a tagged rule block from within a wrapper (like vibe-rules Integration)
 */
export declare function removeTaggedRuleFromWrapper(filePath: string, ruleName: string, wrapperStart: string, wrapperEnd: string): Promise<boolean>;
