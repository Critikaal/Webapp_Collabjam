/**
 * Action handler for the 'vibe-rules load' command.
 * Applies a saved rule to an editor configuration with metadata support.
 */
export declare function loadCommandAction(name: string, editor: string, options: {
    global?: boolean;
    target?: string;
}): Promise<void>;
