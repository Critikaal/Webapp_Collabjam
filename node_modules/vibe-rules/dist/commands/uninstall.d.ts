interface UninstallOptions {
    global?: boolean;
    target?: string;
}
export declare function uninstallCommandAction(name: string, editor: string, options: UninstallOptions): Promise<void>;
export {};
