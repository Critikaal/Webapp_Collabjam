import { RuleProvider, RuleType } from "../types.js";
/**
 * Factory function to get the appropriate rule provider based on rule type
 */
export declare function getRuleProvider(ruleType: RuleType): RuleProvider;
