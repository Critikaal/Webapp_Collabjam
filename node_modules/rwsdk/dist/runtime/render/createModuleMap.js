export const createModuleMap = () => new Proxy({}, {
    get(_, id) {
        return new Proxy({}, {
            get(_, name) {
                return {
                    id,
                    name,
                    chunks: [],
                };
            },
        });
    },
});
